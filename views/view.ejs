<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="icon" type="image/x-icon" href="/ejs.svg">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>View</h1>

    <%- include('partials/link.ejs'); %>

    <div class="center">
    <div>

    <div class="view-title">
    <h3><%= item.item_name %></h3>
    </div>

    <div class="view-item">
    <p><%= item.item_description %></p>
    </div>

    <div class="view-item">
    <p>Quantity: <span><%= item.item_quantity %></span></p>
    </div>

    <div class="view-item">
    <a href="/update/<%= item.id %>">Update</a>
    </div>

    <form action="/delete/<%= item.id %>" method="post">
      <button type="submit" onclick="return confirm('Are you sure you want to delete this item?');" class="btn">
        Delete
      </button>
    </form>
    
    </div>
    </div>

</body>
</html>